'use client';

/**
 * ProjectDetailClient - Client-side wrapper for project detail pages
 * 
 * This is now a simple passthrough component.
 * The PageShell with headerTag="CASE_STUDY" is applied in page.js
 */
export const ProjectDetailClient = ({ children }) => {
  return <>{children}</>;
};

